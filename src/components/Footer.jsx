const Footer = ({ children }) => {
    return (
        <div className="flex justify-center items-center self-stretch h-20 p-4">
            {children}
        </div>
    );
};

export default Footer;
