const Content = ({ children }) => {
    return (
        <div className="max-w-xl mx-auto bg-white rounded-2xl">{children}</div>
    );
};

export default Content;

// flex flex-col max-w-xl justify-center items-center self-stretch bg-white h-80 gap-6 px-6
